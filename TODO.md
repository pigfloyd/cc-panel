总体判断：核心闭环已经成立，状态采集、会话去重、窗口定位这些难点也有较多测试。现在最需要的不是重写架构，而是把它从“能
  用的个人工具”收敛成“不会让人担心的常驻工具”。

  建议按这个顺序改：

  1. 把安装、卸载和健康状态做完整

     首次启动会自动安装 hooks，并且默认开启开机启动，属于比较强的系统改动，但目前没有确认流程。卸载 IPC 已经存在，界面却
     没有入口；单个平台安装失败时，还可能被包装成 ok: true。

     建议增加首次运行引导、Claude/Codex 独立开关、hook 自检、修复和一键卸载，并把自动启动默认改为用户主动选择。相关逻辑
     在 /D:/personal/tpanel/src/main/index.js:69、/D:/personal/tpanel/src/main/config.js:14 和 /D:/personal/tpanel/src/
     main/hook-installer.js:225。

  2. 统一状态模型和界面表达

     当前未提交改动把 Claude/Codex 的 Stop 都改成了 done，方向可以，但需要同步定义清楚：
      - done 是“本轮完成”还是“会话结束”？
      - SessionEnd 当前直接删卡，/D:/personal/tpanel/src/main/sessions.js:106 中并不会进入 README 所说的“已结束，15 秒后
        消失”。

      - ended、dead 有样式但实际基本不可达。
      - 卡片除“工作中”的呼吸点外没有状态文字，其他状态主要依赖背景颜色。
      - /D:/personal/tpanel/src/renderer/app.js:255 会添加 flash class，但 CSS 没有对应动画；大量交通灯 CSS 也没有渲染入
        口。

     建议先画一张明确的状态转换表，再让代码、文档、颜色、声音和测试全部遵循它。卡片上直接显示“工作中 / 等待输入 / 已完
     成 / 出错”，不要只靠颜色。

  3. 修复测试和发布链路

     当前 npm test 在本机 Node v14.21.3 下无法启动，因为 /D:/personal/tpanel/package.json:7 使用了 node --test。改用
     Electron 内置 Node 串行运行后，结果是 61 通过、1 失败，失败项是 Codex hook 启动命令的测试与当前 .cmd 实现不一致。

     更重要的是，/D:/personal/tpanel/test/hook-installer.test.js:28 会直接调用 install()，可能修改真实的 ~/.claude/
     settings.json 和 ~/.codex/hooks.json。测试必须注入临时路径，不能触碰用户配置。

     项目还缺少打包、安装器和升级流程。对于依赖 hook 绝对路径的工具，这是正式分发前的阻塞项。

  4. 强化“等待我处理”的提醒能力
      - Windows 原生通知，点击直接聚焦对应终端
      - 托盘图标和待处理数量
      - “需要输入”和“出错”筛选
      - 可选的“待处理优先”，同时保留现有稳定排序
      - 声音类型或安静时段

  5. 改善常驻与窗口操作体验

     当前关闭主窗口就退出服务，和“常驻副屏”的定位不太一致。建议支持关闭到托盘、明确退出、暂停监听和重新扫描。

     点击卡片现在会把终端移动到主屏中央。最好提供“仅置前 / 移到主屏 / 保持原位置”三种策略，避免破坏用户已经排好的窗口布
     局。

  6. 提高多会话可辨识度和启动效率

     现在主要显示目录 basename，同名项目很容易混淆。可以增加自定义别名、父目录、分支名或终端编号。新建 agent 每次都弹目
     录选择器也偏慢，适合增加“最近目录”和“使用上次目录直接启动”。

  另外，权限处理相关代码、服务端路由、测试和 CSS 仍大量保留，但功能整体被禁用。建议明确二选一：近期完成它，或者删掉这部
  分死代码，减少维护噪声。

  如果只排一个近期版本，我会定为：安装/卸载可控 + 状态模型统一 + 测试隔离。这三项完成后，再做托盘通知和多会话效率功能。
  本次评估未修改项目文件。